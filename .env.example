# claude_code_prd_generator environment config

# Output directory for generated PRDs (used by save_artifact.py hook)
PRD_OUTPUT_DIR=outputs/generated_prds

# MCP servers are configured in .mcp.json, not here.
# See docs/architecture.md for MCP setup instructions.
